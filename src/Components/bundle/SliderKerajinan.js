/*
    Slider  = [1]  = Pancingan 

    ketika berada di sec 1 slider [1] >> slider [3] << 

*/

let Tab = 1;

function Slider(left,on,right, rmleft, rmright,slideValue){

}

function makeSlider(params,Dom){
    if (params === "next"){
        if (Tab == 1){
            Slider()
            console.log(Dom)
        }
    }
}

export default makeSlider
